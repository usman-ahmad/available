require 'spec_helper'

describe Available do
  it 'has a version number' do
    expect(Available::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
